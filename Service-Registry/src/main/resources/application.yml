server:
  port: 8761  # change accordingly

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
    register-with-eureka: false
    fetch-registry: false

spring:
  application:
    name: service-registry